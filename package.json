{
  "name": "tanstack_start_ts",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "preview": "vite preview",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "tsx scripts/seed-car-masters.ts",
    "db:seed:products": "tsx scripts/seed-demo-products.ts",
    "db:import:products": "tsx scripts/import-products.ts",
    "db:import:compatibilities": "tsx scripts/import-compatibilities.ts",
    "db:verify:products": "tsx scripts/verify-product-matching.ts",
    "db:verify:real-catalog": "tsx scripts/verify-real-catalog.ts",
    "db:test": "tsx scripts/test-db-connection.ts"
  },
  "dependencies": {
    "@ai-sdk/openai": "^4.0.9",
    "@hookform/resolvers": "^5.2.2",
    "@radix-ui/react-accordion": "^1.2.12",
    "@radix-ui/react-context-menu": "^2.2.12"
  }
}
